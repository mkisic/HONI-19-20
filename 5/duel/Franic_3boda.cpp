#include <bits/stdc++.h>

using namespace std;

int main()
{
	int n, p, f;
	cin >> n;
	cin >> n >> p;
	cin >> n >> f;
	if(p != f)
		cout << 1 << endl << 1;
	else
		cout << 0 << endl << 0;
	return 0;
}

